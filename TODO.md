# TODO - Multi Model Streamlit Churn Project

- [ ] Step 1: Create `project/` directory structure (data/, models/, visuals/, notebooks/).
- [ ] Step 2: Add `requirements.txt` with pinned dependencies.
- [ ] Step 3: Copy dataset into `project/data/`.
- [ ] Step 4: Implement `project/utils.py` for:
  - [ ] data loading + preprocessing pipeline
  - [ ] training 6 models
  - [ ] hyperparameter tuning (RF + Gradient Boosting/XGBoost)
  - [ ] evaluation metrics + required plots
  - [ ] saving trained pipelines into `project/models/`
- [ ] Step 5: Implement `project/app.py` Streamlit dashboard with required sections and plots.
- [ ] Step 6: Add `project/README.md` with run instructions + leaderboard summary (placeholder until training run).
- [ ] Step 7: Train once locally and verify `models/*.pkl` exist.
- [ ] Step 8: Run `streamlit run project/app.py` and sanity-check UI + prediction form.

